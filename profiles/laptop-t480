# T480 Laptop Hyprland Profile
# Scripts to run for ThinkPad T480 laptop with Hyprland/Wayland
#
# Lines starting with # are comments
# Empty lines are ignored
# List one script name per line
# Config variables: key=value (will be exported as PROFILE_KEY env var)

# === Configuration ===
hypr.local=files/hypr/local-laptop.conf
hyprpaper.config=files/hypr/hyprpaper-laptop.conf
waybar.config=files/waybar/config-laptop.jsonc
waybar.style=files/waybar/style-laptop.css
aur.packages=cheat-bin,claude-code,clickup,figma-linux-bin,google-chrome,postman-bin,proton-mail-bin,rustdesk-bin,slack-desktop,tailwindcss-cli,xnviewmp,zen-browser-bin,zoom

# === Core System ===
ssh-agent
reflector
pacman
core
fonts

# === Hardware (T480-specific) ===
t480
# keyboard - CapsLock->Esc mapping configured in hyprland.conf (kb_options)

# === Desktop Environment (Wayland/Hyprland) ===
hypr
waybar
hyprpaper
fcitx5
gtk
gsettings

# === Shell & Terminal ===
bash
ghostty
tmux

# === Applications & Development ===
apps
aur  # Must run after 'apps' and 'core' (requires Rust and make to build paru)
neovim

# === Applications ===
signal
tailscale
usb
cheat
pandoc
printing

# === System Updates ===
# Run updates last after all packages are installed
update
