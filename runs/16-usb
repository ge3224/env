#!/usr/bin/env bash

set -euo pipefail

log() {
    echo "[USB] $1"
}

log "Installing USB utilities..."

sudo pacman -S --needed --noconfirm \
    udisks2 \
    udiskie \
    usbutils

log "USB utilities installed!"
log "udiskie can automount USB devices - add it to your startup if needed"
